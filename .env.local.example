# Copy to .env.local (or just use the in-app Settings dialog, which writes to the
# local SQLite DB and takes effect immediately without a restart).

VAULT_PATH=C:\Users\mason\OneDrive\Desktop\Mason-Second-Brain
