# Optional. The app works with zero configuration — set the vault path in the
# in-app Settings dialog instead. These env vars are only fallbacks.

# Path to your Obsidian second-brain vault (differs on Mac vs PC).
VAULT_PATH=C:\Users\mason\OneDrive\Desktop\Mason-Second-Brain

# Where the tiny machine-local SQLite file goes (defaults to %LOCALAPPDATA%).
# MASON_DATA_DIR=
